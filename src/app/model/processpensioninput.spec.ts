import { Processpensioninput } from './processpensioninput';

describe('Processpensioninput', () => {
  it('should create an instance', () => {
    expect(new Processpensioninput()).toBeTruthy();
  });
});
